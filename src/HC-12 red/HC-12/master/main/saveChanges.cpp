#include "main.h"
static uint32_t timeToSave = 0;


void savaChanges() {
	if (millis() - timeToSave <= SAVE_TIME) {
		keyState1;
		keyState2;
		keyState3;
		keyState4;
		keyState5;
		keyState6;
		keyState7;
		keyState8;
	}
}